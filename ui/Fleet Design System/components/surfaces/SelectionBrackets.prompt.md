Wraps anything that is currently selected — a fleet marker in the world, a system node, a roster row. Brackets mean selection and nothing else.

```jsx
<SelectionBrackets inset={-4}><SystemNode name="Kestrel" /></SelectionBrackets>
```
