Fleet has almost no buttons — the map is the interface. Use `commit` only for the irreversible order; everything else is `ghost`.

```jsx
<CommandButton variant="commit" icon="lock">commit warp</CommandButton>
<CommandButton size="sm">recall</CommandButton>
```
