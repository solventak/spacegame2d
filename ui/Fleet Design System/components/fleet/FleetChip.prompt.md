One row per fleet under the selection panel. `state="committed"` shows the lock — reserve it for orders the player can no longer take back.

```jsx
<FleetChip id="04" strength={0.62} drones="880" capitals="2" state="committed" selected />
```
