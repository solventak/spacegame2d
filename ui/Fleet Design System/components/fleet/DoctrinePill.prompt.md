Three per selection: stance, formation, target priority. When a multi-fleet selection disagrees, pass `mixed` — never show one fleet's value as if it applied to all.

```jsx
<DoctrinePill label="stance" value="aggressive" glyph="aggressive" active />
<DoctrinePill label="formation" value="wedge" glyph="wedge" mixed />
```
